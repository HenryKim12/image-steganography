#include "decoder.hpp"
#include <string>

using namespace std;

Decoder::Decoder() {}

string Decoder::decode(cv::Mat image) {
    return "";
}

string Decoder::binary_to_text(string binary) {
    return "";
}
