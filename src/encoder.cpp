#include "encoder.hpp"
#include <string>
#include <iostream>

using namespace std;

Encoder::Encoder() {
    
}

// cv::Mat Encoder::encode(cv::Mat & image, string message) {

// }

void Encoder::encode() {
    cout << "encoding" << endl;
}

string Encoder::text_to_binary(string& text) {

}